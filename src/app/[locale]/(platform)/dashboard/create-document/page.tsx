import { WithEditor } from '@/components/editor';

const Page = () => {
  return <WithEditor classNameEditorWrapper={`bg-white h-a4 w-[796px]`} />; // TODO: remove hardcoded width
};

export default Page;
