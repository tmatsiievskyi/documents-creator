import {
  Italic,
  Bold,
  Underline,
  Strikethrough,
  Play,
  ChevronDown,
  Minus,
  Plus,
  AlignJustify,
  AlignLeft,
  AlignRight,
  AlignCenter,
} from 'lucide-react';

export type TIconKeys = keyof typeof icons;

export const icons = {
  Italic,
  Bold,
  Underline,
  Strikethrough,
  Play,
  ChevronDown,
  Minus,
  Plus,
  AlignJustify,
  AlignLeft,
  AlignRight,
  AlignCenter,
};
